<section class="container userprofile">
  <div class="row text-center">

    <h1><%= @user.handle %></h1>

    <br>

    <% @songs.each do |song| %>
    <p><%= link_to song.title, artist_song_path(song.id, song.user_id) %></p>
    <% end %>
  </div>
</section>
