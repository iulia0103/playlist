<section class="container videoartist">
  <div class="row text-center">
    <h1><%= @song.title %></h1>

    <p>Band: <%= @song.artist.name %> </p>
    <p>Year: <%= @song.year %></p>

    <%= embed(@song.video) %>
  </div>
</section>
